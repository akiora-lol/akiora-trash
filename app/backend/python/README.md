Auth on session create asks if user exists,
if there is a user retrieved, it fills user_id in session, if community server is down gg
Sessions are stored in redids only. Need to track smth specific so we can attach so session instead of creating new one

Community
handles group user club entiites

Search
handles forms

Game
handles tournament game draft entities
